{
  "name": "@opensearch/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run test"
  },
  "dependencies": {
    "@opensearch/contracts": "workspace:*",
    "@opensearch/prompts": "workspace:*",
    "@opensearch/sources": "workspace:*",
    "@opensearch/testkit": "workspace:*"
  }
}
